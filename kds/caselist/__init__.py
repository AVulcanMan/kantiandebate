"""OpenCaseList integration: a better, federated search over the disclosure wiki.

Import the search function explicitly to avoid shadowing the submodule:
    from kds.caselist.search import search
"""
